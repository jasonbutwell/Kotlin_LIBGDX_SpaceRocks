package com.mygdx.game.config

class Settings {

    companion object {
        const val title = "Space Rocks"

        const val screenWidth = 1920
        const val screenHeight = 1080

        const val halfScreenWidth = screenWidth/2
        const val halfScreenHeight = screenHeight/2
    }
}